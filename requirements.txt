flask==3.1.0
gunicorn>=22.0.0
requests==2.32.3
openpyxl==3.1.5
Pillow>=10.0.0

# VW / Audi / Skoda / Seat
weconnect==0.60.11

# Tesla
teslapy==2.7.0

# BMW / Mini
bimmer-connected==0.15.3
aiohttp==3.10.11

# Hyundai / Kia (Bluelink / UVO Connect)
hyundai-kia-connect-api>=3.0.0

# Renault / Dacia
renault-api>=0.2.0

# 2FA / TOTP Login
pyotp>=2.9.0

# WebAuthn / Passkey
webauthn>=2.0.0

# PDF reports
reportlab>=4.0.0

# MQTT / Home Assistant publishing
paho-mqtt>=2.0.0

# Toyota / Lexus (optional — pip install mytoyota)
# mytoyota>=0.10.0

# Nissan Leaf/Ariya (optional — pip install nissan-connect-ev)
# nissan-connect-ev>=0.1.0

# Porsche Connect (optional — pip install pyporsche)
# pyporsche>=0.0.7

# JLR InControl (optional — pip install jlrpy)
# jlrpy>=2.0.0
