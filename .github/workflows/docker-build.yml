name: Build & Push Docker Image

on:
  push:
    branches: [ "main" ]
<<<<<<< HEAD
  workflow_dispatch:  # manuell auslösbar
=======
  workflow_dispatch:
>>>>>>> 0f9cce2916333221e41dd906a3b9120211fcd07a

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/ev-tracker:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/ev-tracker:${{ github.sha }}
<<<<<<< HEAD

      - name: Update Docker Hub description
        uses: peter-evans/dockerhub-description@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: ${{ secrets.DOCKERHUB_USERNAME }}/ev-tracker
          short-description: "EV Ladeprotokoll für VW ID.7 via Home Assistant"
=======
>>>>>>> 0f9cce2916333221e41dd906a3b9120211fcd07a
